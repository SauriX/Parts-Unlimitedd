import { observer } from "mobx-react-lite";
import { Fragment } from "react";

const CashTable = () => {
  return (
    <Fragment>
        
    </Fragment>
  );
};

export default observer(CashTable);
