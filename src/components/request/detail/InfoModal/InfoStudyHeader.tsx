import HeaderTitle from "../../../../app/common/header/HeaderTitle";

const RequestHeader = () => {
  return <HeaderTitle image={"infoStudy"} title="Ficha técnica" />;
};

export default RequestHeader;
